# Projeto API com NodeJs

#### Iniciar o servidor
```shell 
    node .
```

#### Instalar o express
```shell 
    npm i express --save
```

#### Instalar o sequelize
```shell 
    npm i --save sequelize
```

#### Instalar o postgres
```shell 
    npm i --save pg pg-hstore 
```
